<h1>New Subfolder</h1>

<%= form_with(model: [@user, @folder, @subfolder], url: user_folder_subfolders_path(@user, @folder), local: true) do |form| %>

  <% if @subfolder.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@subfolder.errors.count, "error") %> prohibited this subfolder from being saved:</h2>

      <ul>
        <% @subfolder.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

<%= link_to 'Back', user_folder_path(@user, @folder) %>
